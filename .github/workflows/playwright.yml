name: Playwright Tests

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      
      - name: Generate HTML report
        run: |
          mkdir -p reports
          npx playwright test --reporter=html
          mv playwright-report reports/
      
      - name: Convert HTML report to PDF
        run: |
          npm install -g capture-website-cli
          mkdir -p reports
          capture-website reports/report.pdf --full-page --format=pdf playwright-report/index.html
      
      - name: Send Email with Report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: na260438@gmail.com
          password: ufnx rfxm bkqx zngz
          subject: Playwright Test Report
          body: Attached is the Playwright test report
          to: saaravistris@gmail.com
          from: na260438@gmail.com
          attachments: reports/report.pdf