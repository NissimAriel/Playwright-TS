name: Playwright Tests

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Browsers
        run: npx playwright install --with-deps

      - name: Run Playwright tests
        run: npx playwright test

      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30
      
      name: Download and Email Artifact
      on:
        workflow_dispatch:

      jobs:
        download-and-email:
          
          steps:
            - name: List available artifacts
              uses: actions/github-script@v7
              with:
                script: |
                  const artifacts = await github.rest.actions.listWorkflowRunArtifacts({
                    owner: context.repo.owner,
                    repo: context.repo.repo,
                    run_id: context.runId
                  })
                  console.log(artifacts.data.artifacts)
      
      - name: Download Artifact
        uses: actions/download-artifact@v4
        with:
          name: playwright-report
          path: ./downloaded-artifact

      
      - name: Send Email with Report
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: na260438@gmail.com
          password: ufnx rfxm bkqx zngz
          subject: Playwright Test Report
          body: Attached is the Playwright test report
          to: saaravistris@gmail.com
          from: na260438@gmail.com
          attachments: reports/report.pdf